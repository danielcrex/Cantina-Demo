Brand assets (bundled local files — no remote fetch):

- cantina-logo.png  — the Cantina product logo shown in the sidebar top and the
  /login header (background keyed to transparency so it sits on pure white).
  Referenced lowercase (case-consistent for Cloudflare's Linux build). If it is
  missing, the brand falls back to a clean text "Cantina" wordmark.

- arbisu-black.png   — the ARBISU logo used ONLY in the footer credit
  ("Cantina è offerto da ARBISU"). Falls back to a text "ARBISU" wordmark.
